#include "nmspc.hpp"

namespace test {
	int average(int & num1, int & num2) {
		return (num1 + num2) / 2;
	}
	double average(double & num1, double & num2) {
		return (num1 + num2) / 2;
	}
}
